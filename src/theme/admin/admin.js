import {createTheme} from "@material-ui/core/styles";

const adminTheme = createTheme({
	palette: {
		primary: {
			main: '#962c90',
		},
		secondary: {
			main: '#3f32a4',
		}
	}
});

export {adminTheme};
